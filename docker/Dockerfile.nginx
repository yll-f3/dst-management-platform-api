FROM nginx:alpine

# 拷贝 nginx.conf（也可继续用 compose 的卷覆盖）
COPY dst-management-platform-api/docker/nginx.conf /etc/nginx/nginx.conf

# 直接打包前端静态资源
COPY dst-management-platform-web/dist /usr/share/nginx/html

# 说明：ports.config.json 仍通过 compose 的卷挂载提供到 /etc/nginx/ports.config.json